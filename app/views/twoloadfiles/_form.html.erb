<%= form_with(model: twoloadfile) do |form| %>
  <% if twoloadfile.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(twoloadfile.errors.count, "error") %> prohibited this twoloadfile from being saved:</h2>

      <ul>
        <% twoloadfile.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.file_field :lfilename %>
  </div>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_field :comment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
